<%= form_for(@company) do |f| %>
  <% if @company.errors.any? %>
    <div id="error_exp;anation">
    <h2><%= pluralize(@company.errors.count, 'エラー') %>saved</h2>

    <ul>
    <% @company.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :会社名 %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :住所 %><br>
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :電話番号 %><br>
    <%= f.text_field :tel %>
  </div>
  <div class="field">
    <%= f.label :ファックス %><br>
    <%= f.text_field :fax %>
  </div>
  <div class="field">
    <%= f.label :Webサイト %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit "更新する"%>
  </div>
<% end %>
