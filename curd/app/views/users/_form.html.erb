<%= form_for([@company, @group, @user]) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, 'エラー') %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :氏名 %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :メールアドレス %><br>
    <%= f.text_field :mail %>
  </div>
  <div class="field">
    <%= f.label :電話番号 %><br>
    <%= f.text_field :tel %>
  </div>
  <div class="actions">
    <%= f.submit "更新する" %>
  </div>
<% end %>

